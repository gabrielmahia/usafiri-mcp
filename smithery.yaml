# Smithery configuration for usafiri-mcp
# East Africa AI Coordination Infrastructure — AI-KungFU
startCommand:
  type: stdio
  configSchema:
    type: object
    properties: {}
    required: []
  commandFunction: |-
    (config) => ({
      command: "usafiri-mcp",
      args: [],
      env: {}
    })
